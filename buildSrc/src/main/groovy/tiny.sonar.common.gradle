plugins {
    id 'org.sonarqube'
}

sonarqube {
    properties {
        property "sonar.projectKey", "com.codeaffine.tiny"
        property "sonar.organization", "code-affine"
        property "sonar.host.url", "https://sonarcloud.io"
        property "sonar.sourceEncoding", "UTF-8"
    }
}
