plugins {
    id("com.codeaffine.tiny.java.modules")
    id("application")
}

project.description = "Standalone runner for RWT application usage demo"

application {
    mainClass = "com.codeaffine.tiny.demo.DemoApplication"
}

run {
    standardInput = System.in
}
